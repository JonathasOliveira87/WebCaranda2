from django.apps import AppConfig


class PaneluserConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'panelUser'
    verbose_name = 'Painel DO Usuário'  # Defina o nome que você deseja aparecer no painel administrativo
